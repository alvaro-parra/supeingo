{
  "name": "supeingo",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:full": "TD=$(mktemp -d) && trap 'rm -rf \"$TD\"' EXIT && npm --prefix \"$TD\" i jspdf@2.5.2 --silent --no-package-lock && NODE_PATH=\"$TD/node_modules\" node --test tests/*.test.js"
  }
}
